interface:
  display_name: "Forexscalpper"
  short_description: "Repo-specific patterns and workflows for forexscalpper"
  default_prompt: "Use the forexscalpper repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true
